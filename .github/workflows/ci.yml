name: CI
"on": [push]
jobs:
 hello:
   runs-on: ubuntu-latest
   steps:
    - uses: actions/checkout@v4
    - run: ls -la
    - run: python --version
    - run: echo "the repo is here and python exists"
    